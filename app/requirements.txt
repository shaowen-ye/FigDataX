# FigDataX Desktop app dependencies.
# Bootstrap:  bash app/run_dev.sh   (creates app/.venv and launches)

# GUI
PySide6>=6.7

# Engine runtime (the app imports scripts.figdatax from the repo root)
opencv-python>=4.8
numpy>=1.26
pandas>=2.0
scipy>=1.11
openpyxl>=3.1

# PDF pipeline (later phases; pypdfium2 is Apache/BSD — MIT-compatible, unlike AGPL PyMuPDF)
pypdfium2>=4.30
pdfplumber>=0.11

# NOTE: the app renders validation overlays natively (OpenCV/QPainter), so matplotlib
# is intentionally NOT an app dependency — it keeps the bundle ~50-80 MB smaller.
