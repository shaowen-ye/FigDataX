# FigDataX skill runtime dependencies
# Bootstrap into the skill-local venv with:  bash scripts/setup.sh
# Version floors reflect what the code is tested against (OpenCV 4.13 / NumPy 2.x).

opencv-python>=4.8
numpy>=1.26
pandas>=2.0
matplotlib>=3.8
scipy>=1.11
openpyxl>=3.1

# NOTE: scikit-image is intentionally NOT required — nothing in figdatax imports it.
